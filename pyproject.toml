[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"

[project]
name = "genai"
version = "0.1.0"
description = ""
requires-python = "<3.15,>=3.10"
readme = "README.md"
license = "Apache-2.0"
dependencies = [
    # OA / open-aea / plugins — pinned exactly (framework contract).
    "open-autonomy[all]==0.21.26",
    "open-aea-ledger-ethereum==2.2.9",
    "open-aea-ledger-cosmos==2.2.9",
    "open-aea-test-autonomy==0.21.26",
    "open-aea-cli-ipfs==2.2.9",
    "open-aea-helpers==0.21.26",
    # Framework/lib deps — ranges (genai is a connection library, not an app).
    "requests>=2.32.5,<3",
    # grpcio: pinned exactly — tight ABI/wheel coupling with protobuf; upstream
    # open-aea / open-autonomy also exact-pin this across the Valory fleet.
    "grpcio==1.78.0",
    "asn1crypto<1.5.0,>=1.4.0",
    "protobuf<6,>=5",
    "hypothesis>=6,<7",
    "ecdsa>=0.15",
    "pytest>=8,<10",
    "pytest-asyncio>=1.3,<2",
    # genai/x402 connection runtime deps (actual imports in packages/).
    "hexbytes>=1.3,<2",
    "typing-extensions>=4.13.2",
    "httpx>=0.23,<1",
    "google-generativeai>=0.8,<0.9",
    "openai>=1.93,<2",
    "pydantic>=2.11.9,<3",
    "pip>=24.0",
]

[project.optional-dependencies]
all = []

[project.urls]
Repository = "https://github.com/valory-xyz/genai"

[dependency-groups]
dev = [
    "setuptools>=78.1.1,<82",
    "tomte[tox, cli, tests] @ git+https://github.com/valory-xyz/tomte.git@v0.7.0",
]

[tool.uv]
default-groups = "all"
prerelease = "allow"

[tool.tomte]
# No non-PyPI upstream beyond OA/open-aea, which tomte auto-derives
# from `[project].dependencies`.
upstream_pins = []
# x402 connection is adapted from coinbase/x402 and is intentionally
# kept outside the lint / docstring / type-hint surface — drop it
# from service_specific_packages so the canonical lint envs don't
# walk it. Same reason its files are excluded from check-copyright.
service_specific_packages_exclude = ["packages/valory/connections/x402"]
tomte_dep_pin = " @ git+https://github.com/valory-xyz/tomte.git@v0.7.0"

[tool.uv.build-backend]
module-name = ["packages"]
module-root = ""
